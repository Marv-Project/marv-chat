/**
 * Shared type definitions used across the monorepo.
 *
 * Add your domain types here, for example:
 *
 * export type ChatMessage = {
 *   id: string
 *   content: string
 *   role: "user" | "assistant" | "system"
 *   createdAt: Date
 * }
 */

export {}
