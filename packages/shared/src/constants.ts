/**
 * Shared constants used across the monorepo.
 *
 * Add app-wide constants here, for example:
 *
 * export const MAX_MESSAGE_LENGTH = 4096
 * export const SUPPORTED_MODELS = ["gpt-4", "claude-3"] as const
 * export const APP_NAME = "Marv Chat"
 */

export {}
